# review-complete (deprecated)

This command file is deprecated. The workflow moved to the Claude Code skill
at `skills/review-complete/` in the openwashdata/pkgreview repository (issue #6).

Install the skills as described in the repository README, then remove this
file from `~/.claude/commands/`. The invocation stays the same:
`/review-complete`.
