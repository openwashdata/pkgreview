# review-pr (deprecated)

This command file is deprecated. PR creation is now part of the
`review-issue` skill (step 7) at `skills/review-issue/` in the
openwashdata/pkgreview repository (issue #6); there is no separate
review-pr skill.

Install the skills as described in the repository README, then remove this
file from `~/.claude/commands/`.
